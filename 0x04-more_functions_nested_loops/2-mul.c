#include "main.h"

/**
 * mul - multiplies two integers
 * @a: first interger to multiply with the second
 * @b: second integer to multiply with the first
 * Return: multiplication result
 */

int mul(int a, int b)
{
	return (a * b);
}
