#include <stdio.h>
#include <stdlib.h>

/**
 * main - Print sigle digit numbers
 * of base 10 from 0
 * Return: Always 0 (Success)
 */

int main(void)
{
	int ch;

	for (ch = 0; ch <= 10; ch++)
		printf("%d", n);
	printf("\n");
	return (0);
}
